# 切り分け用の一時ワークフロー。原因が分かったら削除する。
name: test-reusable

on:
  workflow_dispatch:

jobs:
  call:
    uses: kosuke1997/.github/.github/workflows/autopilot.yml@main
    with:
      issue_number: 1
      issue_body: "切り分けテスト"
      project_name: kaji-wbs
    secrets: inherit
